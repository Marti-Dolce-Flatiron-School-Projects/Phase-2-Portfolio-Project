## MySQL2
default: &default
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("ENV_MAX_THREADS") { 5 } %>
  username: root
  password: Palo5non!

development:
  <<: *default
  database: dev_listings_manager

#
## Warning: The database defined as "test" will be erased and
## re-generated from your development database when you run "rake".
## Do not set this db to the same as development or production.
#

test:
  <<: *default
  database: test_listings_manager

# Remote host
production:
  <<: *default
  database: listings_manager
